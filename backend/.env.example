# Configuration Backend - Travel Quest

# Port du serveur
PORT=5000

# Base de données MongoDB
MONGO_URI=mongodb://localhost:27017/travel-quest
# Ou MongoDB Atlas:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/travel-quest

# Secret pour JWT (CHANGER EN PRODUCTION !)
# Générer avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=votre_secret_jwt_changez_moi_en_production

# Environnement
NODE_ENV=development
# NODE_ENV=production

JWT_EXPIRES_IN=30d

# CORS (origines autorisées)
# CORS_ORIGIN=*
# En production, spécifier l'origine exacte:
# CORS_ORIGIN=https://votre-domaine.com